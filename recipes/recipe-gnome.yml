---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: marschallos-gnome
# description will be included in the image's metadata
description: my personal image which is obviously the best.

# the base image to build on top of (FROM) and the version tag to use
base-image: ghcr.io/ublue-os/silverblue-main
image-version: latest # You can pin to a specific version of Fedora as well

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: rpm-ostree
    repos:
      - https://pkgs.tailscale.com/stable/fedora/tailscale.repo
    install:
      - tailscale

  - type: systemd
    system:
        enabled:
          - tailscaled.service
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /
  - type: gschema-overrides 
  - type: brew
    nofile-limits: true
    brew-analytics: false
  - type: dnf
    repos:
      copr:
        - atim/starship
    install:
      packages:
        - micro
        - starship
        - fastfetch
    remove:
      packages:
        # example: removing firefox (in favor of the flatpak)
        # "firefox" is the main package, "firefox-langpacks" is a dependency
        - firefox
        - firefox-langpacks # also remove firefox dependency (not required for all packages, this is a special case)
        - gnome-software
        - nvtop
        - gnome-tour
  - type: gnome-extensions
    install:
      - 3193
      - 307
      - 8834 # copyous clipboard manager
  - type: signing

  - from-file: flatpaks-all.yml
  - from-file: flatpaks-gnome-specific.yml

  - type: signing # this sets up the proper policy & signing files for signed images to work full
